# --------------------------------------
# FUNCTION fit_regression
# required packages: none
# description:
# inputs:
# outputs:
########################################
fit_regression <- function(x=NULL,y=NULL){

# assign parameter defaults
if (is.null(x) | is.null(y)) {
  x <- runif(10)
  y <- runif(10)
}

# function body

z <- "new ouput"


return(z)

} # end of function fit_regression
# --------------------------------------
# fit_regression()
